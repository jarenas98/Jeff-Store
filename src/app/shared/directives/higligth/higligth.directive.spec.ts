import { HigligthDirective } from './higligth.directive';

describe('HigligthDirective', () => {
  it('should create an instance', () => {
    const directive = new HigligthDirective();
    expect(directive).toBeTruthy();
  });
});
